# Resume-Scorer-Agentic-AI
Agentic Resume Scorer is a FastAPI app where you upload resumes one by one and get an AI-generated match score against a job description. It parses PDF/DOCX files, extracts structured data, and compares it against your JD using a weighted rubric to return a 0–100 score with reasoning.
